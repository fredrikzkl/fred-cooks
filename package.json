{
  "name": "fred-cooks",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "format": "node format.js",
    "watch": "node watch.js",
    "serve": "python3 -m http.server --directory dist 8000",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1",
    "marked": "^15.0.0"
  }
}
